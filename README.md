cd# Diplom_2, Шаклеин Сергей, 24 когорта
# Протестированы эндпоинты API для Stellar Burgers
# Файл test_create_user.py содержит:
# -Тест на создание нового пользователя, ручка: /api/auth/register
# -Тест попытку на создать пользователя, который уже зарегистрирован, ручка: /api/auth/register
# -Тест на попытку создать пользователя и не заполнить одно из обязательных полей, ручка: /api/auth/register - тут использована параметризация для проверки каждого из обязательных полей
# Файл test_login_user.py содежит:
# -Тест на вход под существующим пользователем, ручка: /api/auth/login
# -Тест на попытку входа с неверным логином и паролем, ручка: /api/auth/register, ручка: /api/auth/login (в задании не указано проверять ИЛИ)
# Файл test_create_order.py содержит:
# -Тест на создание заказа с авторизацией, ручка: /api/orders. Поскольку с авторизацией надо было проверять заказ с ингредиентами, без ингредиентов и с неверным хешем ингредиентов, то использована параметризация
# -Тест на создание заказа без авторизации, ручка: /api/orders. Здесь также использована параметризация
# Файл conftest содержит фикстуры для создания/удаления уникального пользователя и дубликата пользователя, который используется также для авторизации 
# Файл data содержит данные для:
# -авторизации под неверным логином и паролем
# -регистрации без одного из обязательных полей
# -Url ручек
# -класс с ResponseBody
# -класс для параметризации проверки заказа с ингредиентами, без ингредиентов и с неверным хешем ингредиентов
# Файл generators содержит генератор email, password и name при помощи Faker
# На описанные тесты сделан отчет Allure